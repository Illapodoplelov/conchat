# Це проста програма для спілкування між кількома користувачами через локальну мережу (localhost).
Вона складається з двох частин: сервера і клієнта.

🖥️ 1. Сервер (server.py)

Сервер приймає підключення від кількох клієнтів і передає повідомлення всім користувачам.

Основні функції:

Запуск сервера: створює сокет і слухає підключення на порту 12345.

Підключення клієнтів: кожен новий користувач отримує повідомлення “<ім’я> joined!”.

Передача повідомлень: усі повідомлення, які надсилає будь-хто, розсилаються всім клієнтам.

Відключення: якщо користувач виходить — сервер повідомляє про це іншим.

Антиспам (частково реалізовано):

якщо користувач надсилає одне й те саме повідомлення понад 5 разів за 10 секунд — його мутить (тобто блокує можливість писати).

💻 2. Клієнт (client.py)

Клієнт підключається до сервера і дозволяє користувачу спілкуватися через консоль.

Основні функції:

Введення імені: при запуску користувач вводить свій нік.

Підключення до сервера: відбувається на адресу localhost:12345.

Надсилання повідомлень: користувач вводить текст у терміналі, і він надсилається іншим.

Вихід: команда exit закриває підключення.

Отримання повідомлень: усі нові повідомлення з сервера показуються у консолі.

Стан “мут”: якщо сервер заблокує користувача за спам — клієнт побачить попередження і більше не зможе надсилати повідомлення.

⚙️ Як працює програма

Спочатку запускається server.py — він чекає на клієнтів.

Потім запускаються client.py (можна кілька разів, щоб симулювати різних користувачів).

Усі користувачі можуть бачити повідомлення один одного.

Якщо користувач спамить — сервер може його зам’ютити.

При виході клієнта інші бачать, що він покинув чат.
